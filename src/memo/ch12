ch12 컬랙션 프레임웍(Collection FrameWork)

1. 컬렉션 프레임웍이란?
- 데이터를 효율적으로 다루기 위한 자료구조 관련 클래스들의 집합
- 클래스 관계도 확인
- Collection 인터페이스를 구현하는 Set, List, Queue 그리고 독자적으로 Map인터페이스가 있다.
2. 제네릭
- 자료형을 정하지 않고 호출하는 곳에서 자료형을 정한다.

3. Set
- 중복없음 , 순서없음

4. List
- 중복 있음, 순서 있음

5. Queue Stack
1) Queue
-먼저 들어온 데이터가 먼저 나간다.
- 예) 은행 대기 순서 
2) Stack
- 나중에 들어온 데이터가 먼저 나간다.
- 예) 웹 브라우저 최근 방문기록, 문서 실행취소

6. Map 
- 키와 값을 쌍으로 저장하는 자료구조
- 예) 우편번호, 전화번호(지역번호)
	1) Map
	2) HashMap
	